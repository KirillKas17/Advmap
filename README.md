# Travel Game Platform (Платформа для геймификации путешествий)

Универсальная платформа для геймификации путешествий с геолокацией, включающая кроссплатформенные мобильные приложения и полнофункциональный backend API.

## 🎯 Описание проекта

Платформа превращает реальные путешествия в увлекательную игру с элементами RPG. Пользователи открывают новые места, собирают артефакты, выполняют квесты, вступают в гильдии и получают достижения за исследование мира.

## ✨ Основные возможности

### Версия 1.0: Базовая функциональность
- 🗺️ **Интерактивная карта** с механизмом "тумана войны" - открытие мест при посещении
- 📍 **Геозоны POI** - автоматическое открытие точек интереса при попадании в геозону
- 🔄 **Офлайн-режим** - полная работоспособность карты и статистики без интернета
- 📱 **Фоновое отслеживание** - автоматическое отслеживание геопозиции в фоне
- 🏠 **Умное определение дома и работы** - автоматическая классификация через кластеризацию DBSCAN
- 🎮 **Система достижений** - разблокировка ачивок за различные действия
- 🔔 **Push-уведомления** - уведомления о достижениях и событиях
- 🛡️ **Защита от GPS-спуфинга** - детекция подделки геолокации

### Версия 2.0: Вовлечение
- 💎 **Артефакты** - коллекционные предметы за посещение мест
- 🎨 **Косметика** - кастомизация профиля и аватара
- 🛒 **Маркетплейс** - торговля артефактами и косметикой между пользователями
- 🎯 **Квесты** - задания для исследования мира

### Версия 3.0: Социальный слой
- 👥 **Гильдии** - объединения игроков для совместных целей
- ✅ **Верификация** - подтверждение посещений мест
- 🎬 **Creator функции** - инструменты для создания контента

### Версия 4.0: AI и продвинутые функции
- 🤖 **AI сервисы** - интеллектуальные рекомендации и персонализация
- 🌀 **Порталы** - специальные точки для телепортации и событий
- 🧠 **Память** - система сохранения важных моментов путешествий

### Версия 5.0: B2B
- 📊 **Аналитика** - детальная статистика и отчёты для бизнеса

## 🏗️ Архитектура

Проект состоит из трёх основных компонентов:

### Backend API (Python/FastAPI)
```
app/
├── api/v1/          # API endpoints
│   ├── auth.py      # Аутентификация
│   ├── location.py  # Геолокация
│   ├── geozone.py   # Геозоны POI
│   ├── achievement.py # Достижения
│   ├── artifact.py  # Артефакты
│   ├── cosmetic.py  # Косметика
│   ├── marketplace.py # Маркетплейс
│   ├── quest.py     # Квесты
│   ├── guild.py     # Гильдии
│   ├── verification.py # Верификация
│   ├── creator.py   # Creator функции
│   ├── ai.py        # AI сервисы
│   ├── portal.py    # Порталы
│   ├── memory.py    # Память
│   └── analytics.py # Аналитика
├── models/          # SQLAlchemy модели
├── schemas/         # Pydantic схемы
├── services/        # Бизнес-логика
└── core/            # Конфигурация и утилиты
```

### Mobile Flutter App
```
lib/
├── core/
│   ├── config/      # Конфигурация
│   ├── database/    # SQLite
│   ├── models/      # Модели данных
│   ├── services/    # Сервисы
│   │   ├── location_service.dart
│   │   ├── background_service.dart
│   │   ├── sync_service.dart
│   │   ├── home_work_detector.dart
│   │   └── push_notification_service.dart
│   └── navigation/  # Навигация
└── ui/
    ├── screens/     # Экраны
    └── widgets/     # Виджеты
```

### Mobile React Native App (альтернативная версия)
```
mobile/
├── src/
│   ├── screens/     # Экраны
│   ├── services/    # Сервисы
│   ├── contexts/    # Контексты
│   └── navigation/  # Навигация
```

## 🛠️ Технологический стек

### Backend
- **FastAPI** 0.104.1 - современный веб-фреймворк
- **SQLAlchemy** 2.0.23 - ORM
- **PostgreSQL** с PostGIS - база данных с поддержкой геоданных
- **Redis** - кэширование и очереди
- **Alembic** - миграции БД
- **Pydantic** 2.5.0 - валидация данных
- **GeoAlchemy2** - работа с геоданными
- **Geopy** - геокодирование и расчёты расстояний
- **Shapely** - работа с геометрией
- **NumPy/Pandas** - обработка данных
- **Python-JOSE** - JWT токены
- **SlowAPI** - rate limiting

### Mobile Flutter
- **Flutter** >=3.0.0 - кроссплатформенная разработка
- **SQLite (sqflite)** - локальная база данных
- **Geolocator** - работа с геолокацией
- **Workmanager** - фоновые задачи
- **Flutter Map** - отображение карт
- **Firebase Messaging** - push-уведомления
- **Dio** - HTTP клиент
- **Connectivity Plus** - проверка подключения

### Mobile React Native
- **React Native** 0.72.6
- **React Navigation** - навигация
- **React Native Maps** - карты
- **React Native Background Geolocation** - фоновое отслеживание
- **Axios** - HTTP клиент
- **AsyncStorage** - локальное хранилище

## 📦 Установка и запуск

### Backend

1. **Требования:**
   - Python 3.11+
   - PostgreSQL 14+ с расширением PostGIS
   - Redis

2. **Установка зависимостей:**
```bash
pip install -r requirements.txt
```

3. **Настройка окружения:**
Скопируйте `.env.example` в `.env` и заполните необходимые переменные:
```bash
cp .env.example .env
```

Основные переменные:
- `DATABASE_URL` - строка подключения к PostgreSQL
- `REDIS_URL` - строка подключения к Redis
- `SECRET_KEY` - секретный ключ для JWT
- `CORS_ORIGINS` - разрешённые источники для CORS

4. **Миграции БД:**
```bash
alembic upgrade head
```

5. **Запуск сервера:**
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

API документация будет доступна по адресу: `http://localhost:8000/docs`

### Docker

Для запуска через Docker:
```bash
docker build -t travel-game-platform .
docker run -p 8000:8000 --env-file .env travel-game-platform
```

### Mobile Flutter

1. **Требования:**
   - Flutter SDK >=3.0.0
   - Android Studio / Xcode

2. **Установка зависимостей:**
```bash
flutter pub get
```

3. **Генерация кода:**
```bash
flutter pub run build_runner build
```

4. **Настройка Firebase:**
   - Создайте проект в Firebase Console
   - Добавьте конфигурационные файлы:
     - `android/app/google-services.json`
     - `ios/Runner/GoogleService-Info.plist`

5. **Запуск:**
```bash
flutter run
```

### Mobile React Native

1. **Требования:**
   - Node.js >=18
   - React Native CLI

2. **Установка зависимостей:**
```bash
cd mobile
npm install
```

3. **Запуск:**
```bash
# Android
npm run android

# iOS
npm run ios
```

## 🔧 Конфигурация

### Backend настройки

Все настройки управляются через переменные окружения (см. `.env.example`):

- **Геолокация:**
  - `GEOZONE_BUFFER_METERS` - буфер для геозон (по умолчанию 50м)
  - `GPS_SPOOFING_THRESHOLD_SPEED_MS` - порог скорости для детекции спуфинга (100 м/с)
  - `GPS_SPOOFING_THRESHOLD_ACCURACY_METERS` - порог точности (1000м)

- **Фоновое отслеживание:**
  - `BACKGROUND_LOCATION_INTERVAL_SECONDS` - интервал обновления (60 сек)
  - `OFFLINE_SYNC_BATCH_SIZE` - размер батча для синхронизации (100)

- **Определение дома/работы:**
  - `HOME_WORK_MIN_VISITS` - минимальное количество посещений (5)
  - `HOME_WORK_MIN_TIME_MINUTES` - минимальное время пребывания (30 мин)
  - `HOME_WORK_CLUSTER_RADIUS_METERS` - радиус кластера (200м)

### Mobile настройки

Разрешения уже настроены в манифестах:
- **Android:** `android/app/src/main/AndroidManifest.xml`
- **iOS:** `ios/Runner/Info.plist`

## 🧪 Тестирование

### Backend тесты
```bash
pytest
pytest --cov=app tests/
```

### Flutter тесты
```bash
flutter test
```

## 📊 Основные модули

### Фоновое отслеживание

Приложение работает в фоновом режиме и периодически фиксирует геопозицию пользователя. При попадании в геозону POI создаётся событие, которое сохраняется в локальную очередь и синхронизируется с сервером при наличии интернета.

### Определение дома/работы

Алгоритм DBSCAN кластеризует точки посещений за последнюю неделю и определяет два самых частых места пребывания. Классификация происходит на основе времени суток и дня недели.

### Геозоны POI

Каждая точка интереса определяется не координатами, а геозоной (полигоном или окружностью), что делает открытие мест более надёжным и удобным.

### Защита от GPS-спуфинга

Система анализирует скорость перемещения и точность GPS сигнала для детекции подделки геолокации. Подозрительные события помечаются и не учитываются в статистике.

### Офлайн-синхронизация

Все события сохраняются локально и синхронизируются с сервером при появлении интернета. Система гарантирует целостность данных и предотвращает потерю информации.

## 🔐 Безопасность

- JWT токены для аутентификации
- Rate limiting для защиты от злоупотреблений
- Валидация всех входных данных через Pydantic
- Защита от GPS-спуфинга
- CORS настройки для контроля доступа
- Хеширование паролей через bcrypt

## 📝 API Документация

После запуска backend сервера документация доступна по адресам:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

## 🗂️ Структура проекта

```
.
├── app/                    # Backend приложение
│   ├── api/v1/            # API endpoints
│   ├── core/              # Конфигурация
│   ├── models/            # SQLAlchemy модели
│   ├── schemas/           # Pydantic схемы
│   ├── services/          # Бизнес-логика
│   └── main.py            # Точка входа
├── lib/                   # Flutter приложение
│   ├── core/             # Ядро приложения
│   └── ui/               # UI компоненты
├── mobile/               # React Native приложение
├── alembic/              # Миграции БД
├── tests/                # Backend тесты
├── test/                 # Flutter тесты
├── requirements.txt      # Python зависимости
├── pubspec.yaml          # Flutter зависимости
└── DOCKERFILE            # Docker конфигурация
```

## 🚀 Развёртывание

### Production рекомендации

1. Используйте переменные окружения для всех секретов
2. Настройте HTTPS для production
3. Используйте reverse proxy (nginx) перед FastAPI
4. Настройте мониторинг и логирование
5. Регулярно обновляйте зависимости
6. Используйте connection pooling для БД
7. Настройте резервное копирование БД

## 📄 Лицензия

Проприетарная

## 🤝 Вклад в проект

Проект готов к продакшену. Все функции реализованы полностью, без TODO и заглушек. При внесении изменений следуйте принципам SOLID и лучшим практикам разработки.
