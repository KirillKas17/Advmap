# Карта Исследователя (Explorer's Map)

Кроссплатформенное мобильное приложение для отслеживания путешествий с фоновым геотрекингом и офлайн-режимом.

## Возможности

- 🗺️ Интерактивная карта мира в стиле фэнтези с механизмом "тумана войны"
- 📍 Автоматическое открытие мест при посещении через геозоны
- 🔄 Полная офлайн-работоспособность карты и статистики
- 📱 Фоновое отслеживание геопозиции для автоматического открытия зон
- 🏠 Умное определение дома и работы через кластеризацию
- 🎮 Геймификация повседневной жизни через ачивки
- 🔔 Push-уведомления о достижениях

## Технологии

- **Flutter** - кроссплатформенная разработка
- **SQLite** - локальная база данных
- **Geolocator** - работа с геолокацией
- **Workmanager** - фоновые задачи
- **Firebase Messaging** - push-уведомления
- **Flutter Map** - отображение карт

## Установка

1. Убедитесь, что у вас установлен Flutter SDK (>=3.0.0)
2. Установите зависимости:
```bash
flutter pub get
```

3. Для генерации кода сериализации:
```bash
flutter pub run build_runner build
```

4. Запустите приложение:
```bash
flutter run
```

## Настройка

### Android

Добавьте разрешения в `android/app/src/main/AndroidManifest.xml` (уже добавлены).

### iOS

Настройте разрешения в `ios/Runner/Info.plist` (уже настроены).

### Firebase

Для работы push-уведомлений необходимо настроить Firebase:
1. Создайте проект в Firebase Console
2. Добавьте конфигурационные файлы:
   - `android/app/google-services.json`
   - `ios/Runner/GoogleService-Info.plist`

## Архитектура

```
lib/
├── core/
│   ├── config/          # Конфигурация приложения
│   ├── database/        # Работа с SQLite
│   ├── models/          # Модели данных
│   └── services/        # Бизнес-логика
│       ├── location_service.dart
│       ├── background_service.dart
│       ├── sync_service.dart
│       ├── home_work_detector.dart
│       └── push_notification_service.dart
└── ui/
    └── screens/         # Экраны приложения
        ├── map_screen.dart
        ├── home_screen.dart
        └── profile_screen.dart
```

## Основные модули

### Фоновое отслеживание

Приложение работает в фоновом режиме и периодически фиксирует геопозицию пользователя. При попадании в геозону POI создаётся событие, которое сохраняется в локальную очередь и синхронизируется с сервером при наличии интернета.

### Определение дома/работы

Алгоритм DBSCAN кластеризует точки посещений за последнюю неделю и определяет два самых частых места пребывания. Классификация происходит на основе времени суток и дня недели.

### Геозоны POI

Каждая точка интереса определяется не координатами, а геозоной (полигоном или окружностью), что делает открытие мест более надёжным и удобным.

## Лицензия

Проприетарная
