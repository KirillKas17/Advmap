# Карта Исследователя (Explorer's Map)

Универсальная платформа для геймификации путешествий с геолокацией, включающая мобильное приложение (Flutter) и REST API (FastAPI).

## 🎯 Основные возможности

### Версия 1.0: Базовые функции
- 🗺️ Интерактивная карта мира в стиле фэнтези с механизмом "тумана войны"
- 📍 Автоматическое открытие мест при посещении через геозоны (полигоны PostGIS)
- 🔄 Полная офлайн-работоспособность карты и статистики
- 📱 Фоновое отслеживание геопозиции для автоматического открытия зон
- 🏠 Умное определение дома и работы через кластеризацию DBSCAN
- 🎮 Геймификация повседневной жизни через достижения
- 🔔 Push-уведомления о достижениях
- 🛡️ Защита от GPS-спуфинга с серверной проверкой аномалий

### Версия 2.0: Вовлечение и игровая механика
- 💎 **Система артефактов** — коллекционные предметы, привязанные к регионам и геозонам
- 🎨 **Косметическая система** — кастомизация аватара с визуализацией прогресса
- 🛒 **Торговая площадка P2P** — обмен артефактами и косметикой, внутренняя валюта
- 🎯 **Сезонные события и квесты** — динамический контент для удержания интереса

### Версия 3.0: Социальный слой
- 👥 **Гильдии и клубы** — сообщества по интересам с ролями и достижениями
- ✅ **Верификация пользователей** — статусы "Проверенный путешественник"
- 🎓 **Платформа для создателей** — инструменты для создания платных квестов (B2B2C)

### Версия 4.0: AI и продвинутые функции
- 🤖 **AI-помощник и планировщик маршрутов** — генерация персонализированных туров
- 💡 **Контекстные рекомендации** — умные подсказки о nearby-достопримечательностях
- 🌌 **Метавселенная воспоминаний** — 3D-лента интерактивной хронологии путешествий
- 🌀 **Порталы/тотемы** — интерактивные точки на карте для взаимодействия

### Версия 5.0: B2B и монетизация
- 📊 **B2B аналитика** — дашборды и экспорт данных для туристического бизнеса
- 🔑 **API ключи** — защищенный доступ к агрегированной статистике

## 🛠 Технологии

### Backend (Python/FastAPI)
- **FastAPI** — современный веб-фреймворк
- **PostgreSQL + PostGIS** — база данных с поддержкой геопространственных данных
- **SQLAlchemy** — ORM для работы с БД
- **Alembic** — миграции базы данных
- **Pydantic** — валидация данных
- **GeoAlchemy2** — работа с геоданными
- **Shapely** — геометрические операции
- **JWT** — аутентификация
- **SlowAPI** — rate limiting
- **Geopy** — вычисление расстояний

### Frontend (Flutter/Dart)
- **Flutter** — кроссплатформенная разработка
- **SQLite** — локальная база данных
- **Geolocator** — работа с геолокацией
- **Workmanager** — фоновые задачи
- **Firebase Messaging** — push-уведомления
- **Flutter Map** — отображение карт

## 📦 Установка и запуск

### Backend

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте переменные окружения (создайте `.env` на основе `.env.example`):
```bash
DATABASE_URL=postgresql://user:password@localhost/dbname
SECRET_KEY=your-secret-key
```

3. Примените миграции:
```bash
alembic upgrade head
```

4. Запустите сервер:
```bash
uvicorn app.main:app --reload
```

API будет доступен по адресу: `http://localhost:8000`
Документация API: `http://localhost:8000/docs`

### Frontend (Mobile)

1. Убедитесь, что у вас установлен Flutter SDK (>=3.0.0)

2. Установите зависимости:
```bash
flutter pub get
```

3. Для генерации кода сериализации:
```bash
flutter pub run build_runner build
```

4. Запустите приложение:
```bash
flutter run
```

## 🔧 Настройка

### Android
Добавьте разрешения в `android/app/src/main/AndroidManifest.xml` (уже добавлены).

### iOS
Настройте разрешения в `ios/Runner/Info.plist` (уже настроены).

### Firebase
Для работы push-уведомлений необходимо настроить Firebase:
1. Создайте проект в Firebase Console
2. Добавьте конфигурационные файлы:
   - `android/app/google-services.json`
   - `ios/Runner/GoogleService-Info.plist`

### PostgreSQL с PostGIS
Убедитесь, что PostgreSQL установлен с расширением PostGIS:
```sql
CREATE EXTENSION postgis;
```

## 📁 Архитектура проекта

```
workspace/
├── app/                          # Backend (FastAPI)
│   ├── api/v1/                  # API endpoints
│   │   ├── auth.py              # Аутентификация
│   │   ├── location.py          # Геолокация
│   │   ├── geozone.py           # Геозоны
│   │   ├── achievement.py       # Достижения
│   │   ├── artifact.py          # Артефакты (v2.0)
│   │   ├── cosmetic.py          # Косметика (v2.0)
│   │   ├── marketplace.py       # Торговая площадка (v2.0)
│   │   ├── quest.py             # Квесты и события (v2.0)
│   │   ├── guild.py             # Гильдии (v3.0)
│   │   ├── verification.py      # Верификация (v3.0)
│   │   ├── creator.py           # Платформа создателей (v3.0)
│   │   ├── ai.py                # AI-помощник (v4.0)
│   │   ├── portal.py            # Порталы (v4.0)
│   │   ├── memory.py            # Воспоминания (v4.0)
│   │   └── analytics.py         # B2B аналитика (v5.0)
│   ├── core/                    # Ядро приложения
│   │   ├── config.py            # Конфигурация
│   │   ├── database.py          # Подключение к БД
│   │   ├── security.py          # JWT и хеширование
│   │   └── logging_config.py    # Логирование
│   ├── models/                  # SQLAlchemy модели
│   ├── schemas/                 # Pydantic схемы
│   ├── services/                # Бизнес-логика
│   └── main.py                  # Точка входа
├── lib/                          # Frontend (Flutter)
│   ├── core/
│   │   ├── config/              # Конфигурация
│   │   ├── database/            # SQLite
│   │   ├── models/              # Модели данных
│   │   ├── services/            # Сервисы
│   │   └── navigation/          # Навигация
│   └── ui/
│       ├── screens/             # Экраны
│       └── widgets/             # Виджеты
├── alembic/                      # Миграции БД
│   └── versions/
│       ├── 001_initial_migration.py
│       └── 002_add_v2_v3_v4_features.py
└── tests/                        # Тесты
```

## 🔌 API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` — регистрация
- `POST /api/v1/auth/login` — вход
- `GET /api/v1/auth/me` — текущий пользователь

### Геолокация
- `POST /api/v1/location/session` — создать сессию
- `POST /api/v1/location/point` — добавить точку
- `GET /api/v1/location/points` — получить точки

### Геозоны
- `POST /api/v1/geozone` — создать геозону
- `GET /api/v1/geozone` — список геозон
- `POST /api/v1/geozone/visit` — зафиксировать посещение

### Достижения
- `GET /api/v1/achievement` — список достижений
- `GET /api/v1/achievement/my` — мои достижения

### Артефакты (v2.0)
- `GET /api/v1/artifact` — список артефактов
- `GET /api/v1/artifact/my` — мои артефакты
- `POST /api/v1/artifact/craft` — создать артефакт

### Косметика (v2.0)
- `GET /api/v1/cosmetic` — список косметики
- `POST /api/v1/cosmetic/equip` — надеть косметику
- `GET /api/v1/cosmetic/avatar` — конфигурация аватара

### Торговая площадка (v2.0)
- `POST /api/v1/marketplace/listing` — создать объявление
- `GET /api/v1/marketplace/listings` — список объявлений
- `POST /api/v1/marketplace/purchase` — купить

### Квесты (v2.0)
- `GET /api/v1/quest/events` — список событий
- `GET /api/v1/quest/available` — доступные квесты
- `POST /api/v1/quest/start` — начать квест

### Гильдии (v3.0)
- `POST /api/v1/guild` — создать гильдию
- `POST /api/v1/guild/join` — присоединиться
- `GET /api/v1/guild/my` — моя гильдия

### Верификация (v3.0)
- `POST /api/v1/verification/request` — запрос верификации
- `GET /api/v1/verification/status` — статус верификации

### Создатели (v3.0)
- `POST /api/v1/creator/apply` — подать заявку
- `POST /api/v1/creator/quest` — создать премиум квест

### AI (v4.0)
- `POST /api/v1/ai/route/generate` — сгенерировать маршрут
- `GET /api/v1/ai/recommendations` — контекстные рекомендации
- `POST /api/v1/ai/conversation` — создать разговор

### Порталы (v4.0)
- `POST /api/v1/portal` — создать портал
- `GET /api/v1/portal/nearby` — найти рядом
- `POST /api/v1/portal/{id}/interact` — взаимодействовать

### Воспоминания (v4.0)
- `POST /api/v1/memory` — создать воспоминание
- `GET /api/v1/memory/my` — мои воспоминания
- `GET /api/v1/memory/timeline` — временная линия

### B2B Аналитика (v5.0)
- `GET /api/v1/analytics/statistics` — статистика
- `POST /api/v1/analytics/export` — создать экспорт

Полная документация API доступна по адресу `/docs` после запуска сервера.

## 🎮 Основные модули

### Фоновое отслеживание
Приложение работает в фоновом режиме и периодически фиксирует геопозицию пользователя. При попадании в геозону создаётся событие, которое сохраняется в локальную очередь и синхронизируется с сервером при наличии интернета.

### Определение дома/работы
Алгоритм DBSCAN кластеризует точки посещений за последнюю неделю и определяет два самых частых места пребывания. Классификация происходит на основе времени суток и дня недели.

### Геозоны
Каждая точка интереса определяется не координатами, а геозоной (полигоном PostGIS), что делает открытие мест более надёжным и удобным.

### Система артефактов
Артефакты выпадают при посещении геозон с определенной вероятностью. Пользователи могут собирать, обмениваться и создавать новые артефакты через систему крафта.

### Квесты и события
Динамические квесты с различными типами задач (посещение мест, сбор артефактов, разблокировка достижений). Прогресс автоматически обновляется при выполнении действий.

### AI-помощник
Генерация персонализированных маршрутов на основе запросов пользователя и контекстные рекомендации о nearby-достопримечательностях.

## 🔒 Безопасность

- JWT аутентификация
- Хеширование паролей (bcrypt)
- Rate limiting для защиты от злоупотреблений
- Защита от GPS-спуфинга с серверной проверкой
- Мультитенантность (изоляция данных по company_id)
- Валидация всех входных данных через Pydantic

## 📊 База данных

Проект использует PostgreSQL с расширением PostGIS для работы с геопространственными данными. Миграции управляются через Alembic.

Применить миграции:
```bash
alembic upgrade head
```

Откатить последнюю миграцию:
```bash
alembic downgrade -1
```

## 🧪 Тестирование

Запуск тестов:
```bash
pytest
```

С покрытием:
```bash
pytest --cov=app
```

## 📝 Лицензия

Проприетарная
